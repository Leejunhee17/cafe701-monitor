services:
  - type: web
    name: cafe701-monitor
    runtime: python
    buildCommand: pip install uv && uv sync
    startCommand: gunicorn --config gunicorn.conf.py --worker-class gthread --workers 1 --threads 4 --bind 0.0.0.0:$PORT server:app
    envVars:
      - key: OCR_API_KEY
        value: helloworld
      - key: POLL_INTERVAL
        value: "8"
      - key: PYTHONUNBUFFERED
        value: "1"
